# Reading Hub 工程行为规范

本文件适用于本仓库中的所有后续修改。它将用户持续提出的协作规范沉淀为项目规则；用户提出新的、长期适用的行为要求时，必须在同一工作中更新本文件，并在交付说明中指出新增或调整的规则。

## 工作原则

- 先确认问题的根因、影响范围和现有行为，再修改实现；不要用仅覆盖当前样例的启发式补丁替代可复用的模型、契约或统一管线。
- 修改保持最小且可维护。优先复用既有抽象；若相同逻辑已在多个位置出现，应抽取共享实现并删除被替代的重复代码。
- 不自行增加用户未要求的操作、提示或选项；确需补充交互设计时先询问用户，避免过度设计。
- 不改变用户未要求的行为或数据。保留已有的本地优先、robots、速率限制、Keychain 和受限原文窗口等安全边界。
- 对不确定的外部状态（站点结构、平台 API、权限、价格、规则）先核实；不绕过登录、验证码、robots、访问控制或反爬机制。

## 每次修改的完成门禁

在声明完成前，按变更影响范围完成下列检查，并如实报告结果、未运行项及原因。先做针对性验证，最终变更稳定后统一执行适用门禁；不默认将每次局部修复升级为全量在线验收。

| 影响范围 | 必做检查 |
| --- | --- |
| 仅文档或协作规范 | `git diff --check`、内容与规则一致性审查；不运行与修改无关的构建、模型调用或应用审计。 |
| 任意代码修改 | `git diff --check`、相关单元/集成测试、`npm run build`，并审查异常处理和错误提示。 |
| CSS、React 布局、标题栏、侧栏、阅读器或 AI 面板 | 运行 `npm run audit:style`；局部改动验证受影响组件的真实 DOM、交互及相关窗口/字号状态。共享字体、样式变量、布局或阅读器展示路径改变时，运行 `npm run audit:visual`，覆盖窄/默认/宽窗口、125% 字号、滚动区、图片、表格、代码、全屏和侧栏状态。 |
| 正文提取、公式、净化、图片代理或阅读器渲染 | 局部修改覆盖问题案例、相邻边界及受影响路径的确定性回归；依赖真实站点的变更核对该来源最新与历史样本。共享提取、净化、媒体或公式管线改变、影响范围无法可靠限定、发布验收或用户要求全量验收时，运行 `npm run audit:reader` 检查每个已订阅来源的最新与历史样本。网络、robots 或远端图片导致的限制必须明确记录，不能伪称通过。 |
| 同步、连接器、数据库、鉴权或调度 | 覆盖新增、去重、发布时间排序、失败退避、重启恢复和权限/令牌失效等受影响路径；确认密钥未进入 SQLite、日志、界面、测试夹具或崩溃信息。 |

测试中发现问题时，应先修复或明确说明阻塞原因；不得仅因原需求完成就忽略回归。

## 修复范围与验证效率

- 动手前用简短说明固定本次问题、必要修改范围与验收标准。根因修复需要的共享调整属于本次范围；无关重构、可选优化和新增功能另列建议，不顺手扩大任务，也不将必要工作拆成未完成的交付。
- 调查中的推测必须标为待验证；追踪实际调用路径后再报告根因，不把文件大小、测试数量或单张截图作为架构缺陷的充分证据。
- 新流程修正与历史数据修复分别列出验收结果。旧稿按授权范围备份、对照和校验后再更新；不为修复历史格式默认重新生成整篇文章，也不扩大到其他用户数据。
- 同一变更版本已通过的检查不重复执行；只有后续相关修改、失败或未解决风险才重跑受影响检查。独立只读检查可并行，依赖同一应用实例、数据库或构建目录的检查顺序执行。
- 本地确定性案例优先覆盖已知问题。真实模型验证仅用于模型交互或生成质量变化，复用授权并优先最小充分案例；不因纯渲染、文档或确定性转换调整而额外调用模型。
- 全量抽检遵循上表触发条件；工具不支持所需筛选时，使用现有针对性测试或最小验证脚本，并说明覆盖边界，不声称执行了不存在的筛选能力。完整长文案例应区分正文提取、资源检查与真实 DOM 验收，首图成功不代表全部媒体成功。
- 在线检查设置有限超时和重试。相同外部失败在没有新证据时不反复尝试，记录限制与受影响验收项；可继续独立工作，但不得将必做且受阻的检查标为通过。
- 对多阶段任务记录调查、实现、历史数据处理、本地验证、在线/模型验证的实际耗时及等待原因；使用工具计时或时间戳，交付时简述主要耗时，不编造比例。超出预期时及时说明原因和剩余工作，不新增产品界面或遥测系统。

## 阅读分类与采集范围

- 阅读导航只保留今日、全部内容、未读和收藏，不再提供新收集、最近删除或历史回填视图。今日以本地自然日为界，显示当天首次收集的文章；新订阅的预览和首次成功同步批次额外要求发布时间也在当天，未知发布时间的首次导入文章不计入今日。首次导入标记必须持久化，失败重试仍属首次导入；重复同步或新增共享来源不改变既有文章的首次收集时间与导入归属。列表、分页和计数必须一致，不伪造发布时间。
- 不再提供历史回填操作或后台归档补抓；升级保留已收集文章及分类选择。取消订阅直接删除信源及其独有文章，包括收藏；仍被其他信源引用的共享文章及其阅读状态保留。启动时按同一规则清理旧的取消订阅记录，不能仅隐藏入口或保留长期归档。
- 网页文章订阅应优先限定到明确的文章分区，并在预览和采集中排除有明确证据的招聘内容；不能以整页重复列表结构代表文章身份。浏览器渲染要求与规则的人工/自动归属独立，跨站的明确文章链接仍允许收集。历史误收清理仅处理证据明确的来源关联，保留收藏及其他来源的共享内容。
- 标题与卡片日期分别提取，不能把独立日期标签拼入标题，也不能删除作者标题本身包含的日期。来源设置只读展示信源类型，不提供“高级设置”和“清理未收藏内容”；取消订阅统一执行上述删除规则，旧隐藏标记不自动恢复。
- 知乎回答须以回答 ID 绑定链接、正文和卡片元数据；多回答页面不得通过全文评分或通用 Readability 选择其他回答，重定向或目标身份不明时明确失败，不以搜索摘要替代原回答。

## 阅读器与视觉质量

- 中英文排版参考 Qiaomu AI RSS：界面与内置阅读正文（包括中文改写）的中文采用随应用打包的朱雀仿宋，英文字体保持原有设置（英文正文 Georgia），缺字回退系统字体，代码和公式保留专用字体；正文默认 19px 和 1.9 倍行高，列表标题采用 1.55 倍行高；保留用户字号与密度设置，标题随字号缩放，避免正文混排挤压。
- 界面保持阅读优先：统一界面字体与按钮样式，正文衬线字体和代码等宽字体按用途区分；字体、按钮尺寸、圆角和主/次/危险操作状态复用共享样式变量，不在各面板重复定义。视觉迭代可参考 PaperRss 与 Qiaomu AI RSS 的克制排版和紧凑工具栏，并保持现有功能与无障碍状态可用。
- 信源内容页不放置重复的同步状态与“来源设置”矩形面板；来源设置沿用侧栏信源右键入口，状态与错误详情放在来源设置中查看。
- 信源图标缺失或加载失败时显示名称的首个完整字符；设置页标题与返回按钮对齐。排版密度与字号只在设置中调整，内置阅读器顶部不重复提供这些控件。
- 所有作者明确给出的 TeX 分隔符必须经统一、安全的解析路径处理；不得依靠“文本看起来像公式”的局部猜测。代码块、转义分隔符和不成对的货币符号必须保持普通文本。
- 公式要在真实 DOM 中检查：行内公式无原始分隔符泄漏；块公式、编号和主体不重叠；超宽公式在自身容器横向滚动，不撑破页面。
- 图片必须保持纵横比并受正文列及最大高度约束；加载失败必须提供安全的原文入口。
- 图片去重应依据同一媒体块的备用图及页面明确声明的尺寸候选，分享卡片和订阅封面仅作为净化后无图片及视频正文的备用插图，正文已有图片或视频时不得额外注入元数据封面，不依赖文件名相同来判断分享图与正文图是否重复；保留不同图注或正文位置中有意复用的图片，不以删除任意 URL 参数或全文全局去重代替图片身份判断。
- 基础 WebM/MP4 视频由用户点击后加载，不自动播放或预取；主进程逐跳校验公共 HTTPS、robots、类型、大小和超时，不携带浏览器凭据。正文切换、关闭及新视频加载时取消旧请求并释放临时媒体；不持久化视频。无法支持或超限的媒体保留图注与原文入口，不泄漏原站“不支持 video 标签”的备用文案。
- 阅读器不得加载或执行原站 CSS/JavaScript；远端 HTML 必须在主进程净化后才进入渲染器。
- 共享阅读展示或正文管线改变时，至少覆盖科学空间、普通博客/RSS、知乎内容和 AI 学习回答；优先使用确定性夹具，外部验证按完成门禁执行。局部且影响可限定的改动仅验证相关类别及边界，并说明范围。

- 语言切换仅展示中文和英文，每种语言最多一个入口；其他语言正文仍可正常阅读，但不提供其语言按钮。只有一个可用中/英文替代版本时也应能切换。
- 文章标题中的自链接保留可见标题文字，仅去除锚点图标；依赖原站样式/脚本的明确图表保留可读说明与原文入口，侧栏旁注转为有标记的正文附注，不把控件或布局碎片当作正文。
- 文章语言版本统一支持独立页面和同页正文；以作者声明的语言属性、标签页关联及成组语言容器为依据，不依赖特定域名或执行原站脚本。先选择单个版本，再走统一净化、图片和公式管线；同页语言不沿用页面级 lang 覆盖正文语言。主进程校验页面与语言组合，切换后重新确认版本存在，不将双语全文持久化，也不以不同语言片段或普通代码标签页猜测整篇翻译。

## AI 模型发现

- 模型选择不依赖随应用发布的固定模型白名单；优先从本机 Codex App Server 或已配置供应商的模型接口发现，Codex 推理强度使用所选模型公布的能力。
- 保留显式刷新、短期本地缓存、API 模型手动输入及跟随 Codex 默认配置的入口；目录刷新失败或模型暂未列出时，不自动替换或清空已保存选择。密钥变更后失效旧目录，拒绝过期响应覆盖新配置。
- 模型发现不创建问答任务、不发送文章正文；API 凭据仅由主进程读取并发送给对应供应商。目录接口有超时、大小与分页限制，退出时取消请求并排空本地进程。

## 中文改写

- 中文改写由用户逐篇主动触发，与信源同步和 AI 学习问答独立；设置中单独选择供应商、动态发现或手动输入模型，复用 Keychain 凭据，不改变问答模型。
- 任务在主进程异步排队并持久化，切换文章不取消任务；应用退出取消并排空运行中的请求，保留排队任务，重启时将中断任务标记为可手动重试，避免自动重复调用。重复点击合并任务，设置变更不覆盖已排队任务的模型快照。
- 正文经现有受限读取与净化管线取得，只有摘要时不冒充完整正文；长文按完整段落、代码和公式分段，不静默截断。只有全部分段成功才原子替换本地改写，失败、取消和重新生成期间保留旧稿。
- 改写输入通过统一的结构转换保留标题、列表、表格、引用、代码及图注；分段使用与显示端相同的 Markdown 语法，不先压成纯文本。资产绑定所属正文块，禁止生成过程跨块移动或重复；程序检查结构，模型只改写语言，不增加默认模型审阅或新界面控件。
- 推荐卡片仅依据同一容器内的标题、封面和阅读入口等明确关系整理为一个标题链接，不对全文相同网址去重。旧稿修复须同时具备原文卡片证据和相邻卡片碎片证据，不跨正文合并；跨旧生成分段的同一卡片统一处理。验收需贯穿净化正文、转换、生成、恢复和真实 DOM，覆盖标题层级、嵌套列表、表格、图注、代码缩进及重复卡片。
- 默认改写每节一次生成，短文一节完成；长文携带精简的已生成上下文统一用词和衔接，不强制提纲、审阅或多轮修订。完整生成后立即保存可读版本，模型的衔接或文风建议不得阻断保存。空响应、输出截断及大小限制仍属于技术失败，不能把部分稿冒充完整文章。
- 完成分段逐节保存为本地恢复记录；手动重试仅复用原文指纹、标题、地址、模型设置与提示版本一致的记录，不自动重复生成已完成分段。恢复记录仅含派生中文及不含原文正文的结构索引，取消、重启和迟到响应不得越过任务身份校验；删除改写或独有文章同时删除恢复记录。
- 原文/中文改写在收藏工具栏最左侧使用两个无边框文字标签，当前版本以下划线标识，不弹出菜单或遮挡正文；工具栏图标统一尺寸与样式。选择缺失版本不调用模型，在正文区保留生成入口。完成后只显示标题与正文，不显示免责声明、未检查提示、模型/时间、本地保存标签，以及重新生成、删除、检查改写按钮。
- 改写保留已有 TeX；原文以普通字符书写的数学表达式由改写模型结合语义输出带分隔符的 TeX，保留上下标分组，不在显示端猜测普通文本为公式。改写输入保留标准 Markdown 链接与图片位置，网址不与中文标点混写；显示端使用标准链接语法解析，图片复用主进程代理、取消和安全边界，不能直接加载任意模型图片地址。改写错误提示后不放置“改写设置”按钮，模型配置沿用设置页。质量流程变更用固定非私密材料做真实模型评估并记录限制。

- 改写作为带模型、生成时间、原文地址和内容指纹的本地派生文档保存，由版本选择标识为中文改写，不覆盖原文或发布它；不把改写当作作者提供的中文版本。取消订阅删除独有文章时一并删除改写，共享文章仍保留；迟到请求不得重建已删除的改写。

- 改写的链接目标、图片、显式 TeX、公式编号和引用由程序保管，以稳定标记交给模型；模型只翻译对应锚文本并改写正文，之后本地恢复原始结构，不增加逐节模型审阅。旧稿修复须对照原文链接与锚文本，只有唯一匹配的中文语句才附加链接，否则使用原文锚文本的翻译，禁止把链接机械挂到前几个字。图片备用地址须有原文媒体块的明确关联；不能通过任意删改 URL 参数猜测。
- 中文改写可使用 AI 对话与划词操作，对话读取当前版本，切换版本时关闭旧对话并取消请求。

## 代码健康与安全审查

每次功能修改后执行一次有针对性的审查：

- 用快速搜索检查废弃分支、重复函数、未使用导入、过时测试和已被替换的样式/选择器；删除确认无引用的冗余代码，而不是保留死代码作为“保险”。
- 检查状态竞态、重复点击、异步过期响应、空数据、网络失败、重试和窗口关闭等边界情形。
- 检查 IPC 是否最小授权，外部 URL 是否经过公共 HTTPS 校验，HTML 是否净化，敏感信息是否只存在 Keychain/内存中。
- 不使用破坏性 Git 或文件操作；不提交凭证、Cookie、数据库、构建产物、审计全文或本地用户数据。
- 用户明确退出应用时，一次操作必须启动并完成退出流程：先阻止新窗口和新任务，取消并排空已接收的工作，等待初始化和数据库写入安全结束，再关闭窗口；原文页面的卸载回调不得阻止应用退出。

## 交付与版本控制

- 交付前检查工作区变更只包含本任务内容，并列出主要修改、验证命令与任何已知限制。
- 除非用户另有要求，在完成验证后创建语义清晰的 Git commit，并推送到已配置的远程仓库；提交前不覆盖用户已有的未提交变更。
- 最终答复以结果为先，简洁说明根因/方案、验证情况、提交哈希，以及开发环境是否需要重启。

- 用户已明确确认 `zeract/reading-hub` 为本人仓库，授权向已配置的该远程仓库 `main` 分支推送通过验证的修改。

## 维护本规范

当用户提出新的长期行为规范时：

1. 将规则加入最贴近的章节；必要时新建章节。
2. 避免与用户当次明确指令冲突；当次指令优先。
3. 将 `AGENTS.md` 的更新与实现一并测试、提交和推送。

## 正文结构与迁移契约

- 中文改写保存独立格式版本、正文块标识与偏移、源/目标指纹及资产归属；原文正文不因结构索引而额外持久化。新生成关系来自实际生成校验，旧稿只恢复可证明的派生结构，未知源关系不猜测。
- 旧稿格式迁移保持正文不变、幂等并保留一份随改写级联删除的原始结果备份；不支持的新版本不降级覆盖。单条损坏任务不得阻塞其余队列，恢复记录不能导致无故重跑已完成分段。
- 原文图片回退和中文改写图片共用可视区域请求调度、取消及失败诊断；网络并发有界，暂时性失败仅有限重试，robots、永久 HTTP 错误及类型/大小限制不自动重试。不得用新增按钮或扩大缓存替代加载生命周期修复。

- 改写输出协议须明确区分必留的结构标记与不输出的说明字段，不能给出相互冲突的指令；允许剥离完整包住全部预期正文块的 Markdown 外围围栏，不丢弃任意解释文字、不猜补缺失块。失败诊断仅记录节号、程序生成的块标识和固定类别，不保存模型正文或敏感响应。
- 提示词版本默认隔离恢复记录；仅在改动纯属输出封装、正文转换和内容校验语义保持兼容时，显式列出可复用的旧协议版本，并继续核对原文、标题、地址与模型设置。不能因提示词文字调整就无故重跑已经验证成功的分节。

- 资产输出协议不能把正确的标准 Markdown 代码表示误判为内容丢失。仅在同一块内、Markdown 解析后的行内代码值与数量均能严格对应时，可恢复为原始代码资产；不从普通文字猜测代码，不修复被改名的标识符，不借用其他块的资产。资产错误须指出程序生成的资产 ID、种类与缺失/重复等固定原因。

- 改写正文块使用单向开始标记与整节结束标记，避免要求模型逐块复制成对结束符；完整代码、图片、公式等不可改写块由程序按原位恢复，不要求模型返回。正文中原样返回的资产仅在同块内内容与数量严格一致时接受，禁止把只读上下文中的代码复制到其他正文块；纯资产分节不调用模型，仍遵守取消和检查点门禁。
