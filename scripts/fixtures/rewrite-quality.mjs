// Authored, non-private material. Anchors are smoke checks, not semantic ground truth.
export const rewriteQualityCases = [
 {id:"blog",title:"A cache result with limits",anchors:["80","100","20","40"],text:`A small team evaluated a request cache on one staging server. Median latency fell from 100 ms to 80 ms across 20 runs, but the team did not measure tail latency. This observation does not show that the cache improves every workload.\n\nThe request cache stores responses for 40 seconds. A cache hit returns the saved response; a cache miss still calls the original service. The experiment deliberately excluded authenticated responses, so its results do not justify caching private user data.\n\nThe author recommends measuring error rates before rollout. The team has not yet tested production traffic. The result is a useful initial observation, not evidence that the cache is safe to enable everywhere.`},
 {id:"long",title:"Queue maintenance across several experiments",anchors:["12","48","320","64"],text:[
 `This report separates queue capacity from service capacity. A waiting queue buffers requests; it does not make a worker faster. The measurements used 12 workers and 48 client connections. The team did not measure whether these results generalize to other machines.`,
 ...Array.from({length:18},(_,i)=>`Observation ${i+1}: the waiting queue was examined during a separate controlled interval. A producer submitted work to the scheduler, and the scheduler assigned accepted work to an available worker. When the queue filled, new submissions were rejected explicitly; the system did not claim they had been accepted. The operator recorded both completed jobs and rejected submissions. Keeping these counts separate matters because a lower completion latency can otherwise conceal dropped work. This observation concerns the same waiting queue throughout the report, not a second independent queue.`),
 `Across the experiment, queue capacity was 320 jobs, whereas each worker processed at most one job at a time. The waiting queue therefore cannot be interpreted as 320 simultaneous executions.`,
 `In a later configuration, capacity was reduced to 64 jobs. This change limited waiting time but increased rejections in the recorded workload. The author does not recommend it as a universal default: capacity must be chosen together with the caller's retry policy and the worker's actual throughput.`].join("\n\n")},
 {id:"science",title:"成功比例与结论边界",anchors:["80","100","0.8","20"],text:`我们在一次固定条件的实验中执行了 100 次请求，其中 80 次成功、20 次失败。成功比例是描述这批记录的统计量，并不等于未来每次请求都会以相同比例成功。\n\n$$\np=\\frac{80}{100}=0.8\n$$\n\n这里的分母包含失败请求，不能把分母改成成功请求数。我们没有建立独立同分布假设，也没有给出置信区间，因此不能仅凭这个结果声称两个模型有显著差异。\n\n\`\`\`python\nsuccess_rate = 80 / 100\n\`\`\`\n\n代码只是计算已观察到的比例，没有模拟新的实验，也没有自动检验因果关系。中文改写需要保留这些限制条件，不能将“本次实验观察到”扩大为“已经证明普遍成立”。`}
];
