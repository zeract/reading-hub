# 改写响应封装与有限恢复

2026-09-13：本地任务记录显示 Representing the Physical World through Structured Language 使用 DeepSeek/deepseek-v4-flash，第 1/2 节已保存，第 2 节 JSON 解析失败。失败原始响应未持久化，因此不能确认该次具体是围栏格式还是 JSON 语法错误，也不能将推测当成站点特例。

发现当前结构化协议解析仅兼容带 json 标签的反引号围栏，且 JSON 错误绕过已有一次局部恢复预算。修复统一接受完整 JSON 或单个完整的 JSON/无标签反引号、波浪线外围围栏。仍拒绝解释文字、多个对象、无效转义和残缺响应，不提取字符串中的局部对象或猜补正文。

JSON 语法与封装异常使用固定类别 json-syntax/json-envelope。当前节最多重新请求一次，与漏段和行内引用修复共用预算；从原节输入生成，不将坏响应发送回模型。之前保存并通过校验的分节不重跑。传输失败、取消、输出截断和大小限制保持既有失败边界。连续失败明确提示已重试一次。

未改变提示内容、段落分组、源指纹或检查点版本，旧成功记录可以继续使用。没有修改特定站点提取和渲染路径，也没有新增界面入口、后台审阅或原始响应日志。

验证：npm run build、git diff --check 通过；相关四个测试文件 53 项通过，覆盖封装、字符串转义、多个对象、错误响应、两节恢复、引用共享预算以及传输错误。构建与定向测试约 01:07 完成（测试 2.46 秒）。本次不涉及展示/正文提取，不重复在线信源审计。真实失败节模型测试已请求授权，尚未执行；不能称为该文章真实模型验收通过。

## 行内引用反馈（2026-09-13）

后续 N259.text 失败定位到参考文献 li：普通 span 中的编号 04，加一个文献链接。现有协议只给出标记文本，没有标签类型或嵌套说明；局部修复还会重复发送原请求，不告诉模型哪一个标记以何种方式失效。此处不表示已知历史响应确实漏了编号；历史原始响应没有保存。

现在按当前段落给出有界的引用说明（类型、父级、准确开闭标记，总记录内容最多 6000 字符），正文中未列入说明的标记仍须保留。校验区分 missing、duplicate、nesting、token-kind、unclosed、empty 和 syntax，局部修复携带程序生成的段落/引用 ID 与固定类别。未知模型标记只显示 unknown，避免错误提示保存原始输出。分节、源指纹、持久化格式及一次局部修复预算未改变。

真实验收经用户授权只发送失败的第 2 节。缓存原文转换后的指纹与数据库检查点完全一致，复用第 1 节。第 2 节有 24 段、43 个引用；首次输出在 N184 中重复 N185，新反馈仅请求该段修复后成功，共 2 次模型请求、8.1 秒。完整合并稿的所有元素标签/属性、链接与资产与源文档一致，N259 的编号及原始目标链接保留。结果仅保存在临时副本，未替换用户稿件。最终有界说明保留该节全部 43 项（序列化约 4270 字符）。

最终 build、diff --check 与相关四文件 55 项测试通过（测试 2.47 秒，约 10:09 完成）。新增编号/普通包装反馈和未知标记隐私边界回归。没有改变提取或渲染，不重复全来源/视觉审计。
