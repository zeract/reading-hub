# 开发产物与分层验证落地

状态：完成（本地验证通过，远端 CI 未核实）

## 目标与范围

落地轻量任务模板、统一验证入口、中文改写分层回归；复用现有本地测试与 CI，不安装 hook，不改变产品交互、正文协议或用户数据库。

## 根因与契约

现有 CI 已运行全套测试、构建和样式检查，但没有统一的范围/版本证据报告。历史验收存在源树往返一致却未确认原文表格语义的缺口。实现采用显式范围选择与四层案例，不用路径自动推断所有风险。

当前有效流程见 [开发与验收](../development-workflow.md)。任务模板仅用于有必要的多阶段工作，不要求每阶段新文档。新增 AGENTS 规则同步明确这些约束。

## 实施与验收

| 预期行为 | 实现/回归入口 | 验收方法 | 结果及证据 |
| --- | --- | --- | --- |
| 失败不继续、源变化不能冒充通过 | scripts/verification/plan.test.mjs | 临时 Git 仓库执行真实 runner、模拟子命令 | 最终 7 项通过，含未跟踪文件的空白错误 |
| 改写保留的是正确识别的结构 | test/rewrite-layered-contract.test.tsx | 独立源语义断言→模拟生成→保存读取→React jsdom | 已随 core 全套测试通过 |
| CI 与本地使用同一入口 | .github/workflows/ci.yml | 配置审查、本地 core 验证 | 远端运行需推送后核实 |

## 当前进度与交付

- 已实现模板、文档、四层矩阵、单次构建调度和有限超时；报告记录 commit/sourceHash/平台/耗时/未运行项，无正文和密钥。
- 新工作区缺依赖；离线安装因缓存不足失败，正常安装补齐。依赖安装属于环境等待，不是测试执行时间。
- 本次只修改开发工具、文档和测试，没有改变正文展示或模型交互，因此不额外执行在线来源、真实模型、窗口视觉或旧稿写回。CI 的 Ubuntu 结果不代表 macOS 安装包验收。
- 最终验证记录：`.verification/latest.json`（不提交，CI 以 artifact 留存）。core 报告另存 `.verification/core.json`；验证后只更新本任务记录，另做文档 diff 检查，不重复代码测试。
- 应用不需要重启。hook 和分支保护均未变更。

## 最终验证记录

- 2026-09-13 10:30:34—10:31:01（上海时间）：`npm run verify` 通过；工作流回归 1.36 秒、全部项目测试 20.45 秒、构建 4.57 秒、样式 0.10 秒。代码在检查期间未变化。
- 已验证源指纹：`8855ca350a8e4c136addb73f86864fb35a71a7798a4f7575c52fe536675ce2ef`（基于提交 `2cc59e8` 加本次工作树）；之后只更新此验收文档。实际实施起始未单独计时，不推算总耗时；安装依赖约 1 分钟。
- 首轮新链路测试对单独 ArticleBody 的图片 src 预期不正确：图片请求拦截由上层媒体生命周期负责，已把本案例限定为图片归属保留，真实代理/取消由现有媒体测试覆盖。没有为通过测试修改产品实现。
- 验证工具首轮发现 Git 的 no-index 空白检查对正常新文件也返回差异码 1；现已区分正常差异与空白错误，并增加新文件正反例。
- `npm audit` 报告现有开发工具链 6 项告警（3 high、2 moderate、1 low），涉及 electron-builder 的 @xmldom/xmldom、fast-uri、js-yaml，wait-on 的 joi，以及 vitest/@vitest/mocker。本次未更改依赖版本；这些告警未修复，不宣称安全审计通过，需另行评估升级兼容性。
